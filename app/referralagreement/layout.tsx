export const metadata = {
  title: "Referral Agreement | Gildre",
  description: "Review and acknowledge the Gildre Referral Program Agreement.",
};

export default function ReferralAgreementLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
