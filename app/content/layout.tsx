export const metadata = {
  title: "Founder Content | Gildre",
  description:
    "Blogs, guides, and resources for founders at every stage — from validating your idea to scaling past $5M ARR.",
};

export default function ContentLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
